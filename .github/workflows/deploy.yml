name: deploy

on:
  workflow_dispatch:

jobs:
  placeholder-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TODO: add deployment workflow in Phase 2+"
